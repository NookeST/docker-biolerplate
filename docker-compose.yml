version: '3.3'
services:
  nginx:
    build:
      context: ./docker
      dockerfile: ./nginx/Dockerfile
    ports:
    - 8080:80
    volumes:
      - type: bind
        source: .
        target: /app
